rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Improved security rules for logout and profile management.
    
    function isAuthed() {
      return request.auth != null;
    }

    function isAdmin() {
      return isAuthed() && 
             exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    function isTeacher() {
      return isAuthed() && 
             exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'teacher';
    }

    // প্রতিষ্ঠানের তথ্য সবাই দেখতে পারবে
    match /school/info {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ইউজার প্রোফাইল রুলস
    match /users/{userId} {
      allow get: if isAuthed() && (request.auth.uid == userId || isAdmin());
      allow create: if isAuthed() && request.auth.uid == userId;
      // Allow users to update their own online status even when logging out
      allow update: if isAuthed() && (request.auth.uid == userId || isAdmin());
      allow list: if isAuthed(); 
      allow delete: if isAdmin();
    }

    // সাধারণ রিসোর্স যা সকল লগইন করা ইউজার পড়তে পারবে
    match /classRoutines/{id} {
      allow read: if isAuthed();
      allow write: if isAdmin();
    }

    match /notices/{id} {
      allow read: if isAuthed();
      allow write: if isAdmin();
    }

    match /holidays/{id} {
      allow read: if isAuthed();
      allow write: if isAdmin();
    }
    
    match /exams/{id} {
      allow read: if isAuthed();
      allow write: if isAdmin();
    }

    // শিক্ষার্থী ও শিক্ষক ডাটা
    match /students/{id} {
      allow read: if isAuthed();
      allow write: if isAdmin();
    }
    
    match /staff/{id} {
      allow read: if isAuthed();
      allow write: if isAdmin();
    }

    // হাজিরা, রেজাল্ট এবং মেসেজ লগ (শিক্ষক ও এডমিন উভয়েই পারবে)
    match /attendance/{id} {
      allow read: if isAuthed();
      allow write: if isAuthed();
    }
    
    match /results/{id} {
      allow read: if isAuthed();
      allow write: if isAuthed();
    }

    match /messageLogs/{id} {
      allow read: if isAuthed();
      allow write: if isAuthed();
      allow delete: if isAdmin();
    }

    match /emailLogs/{id} {
      allow read, write: if isAuthed();
    }

    // হিসাব-নিকাশ
    match /feeCollections/{id} {
      allow read: if isAuthed();
      allow write: if isAuthed();
    }

    match /transactions/{id} {
      allow read: if isAuthed();
      allow write: if isAuthed();
    }
  }
}